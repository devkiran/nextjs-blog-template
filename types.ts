export type Project = {
  name: string;
  description: string;
  image: string;
};

export type Post = {
  title: string;
  description: string;
  publishedAt: string;
  slug: string;
  content: string;
};
